pipeline {
    agent any
    stages {
    	stage('init') {
    		steps {
    			echo 'Hola mundo'
    		}
    	}
        stage('finish') {
        	steps {
        		echo 'Done'
        	}
        }
    }
}

